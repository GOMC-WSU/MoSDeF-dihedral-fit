import numpy 
import pytest 

from mosdef_dihedral_fit.tests.base_test import BaseTest

class TestMathOperations(BaseTest):
    def test_round_to_sig_figs():
        values = []
        sigfigs = []
        for value in values:
            for sigfig in sigfigs:
                assert True


    def normalize_vector_length(vector):
        return None 
    
    def normalize_vector(vector):
        return None